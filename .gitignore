
# Dependencies (both backend & frontend)
node_modules/
# Keep package-lock.json? Usually commit it, but you can uncomment if you prefer to ignore:
# package-lock.json
yarn.lock
pnpm-lock.yaml

# -------------------------------
# Environment variables (sensitive)
.env
.env.local
.env.*.local
backend/.env
frontend/.env
*.env

# -------------------------------
# Build outputs / production
dist/
build/
backend/dist/
frontend/dist/
frontend/build/
out/
.next/
.nuxt/
.vite/

# -------------------------------
# Logs and temp files
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
backend/uploads/   # temporary uploaded files
backend/test/data/ # pdf-parse test folder (if still present)

# -------------------------------
# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# -------------------------------
# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# -------------------------------
# Testing / coverage
coverage/
.nyc_output/
*.lcov

# -------------------------------
# Debug / misc
.cache/
.parcel-cache/
.env.test
*.tsbuildinfo

# -------------------------------
# Optional: ignore MongoDB data directory if local
data/
mongodb-data/

# -------------------------------
# Keep the .gitignore itself
!.gitignore